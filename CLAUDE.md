# CLAUDE.md

> Note: This placeholder was created because the CLAUDE.md file referenced by the user was not present in the uploads. Replace this content with the intended document that describes the LATER backend phase (Neon + Netlify Functions + Cloudinary).

The current Lovable project is **frontend-only**. All data is served from
local JSON fixtures in `src/data/` and all write actions go through
stub functions (see `src/data/api.ts`). No Supabase / Lovable Cloud is
enabled.
